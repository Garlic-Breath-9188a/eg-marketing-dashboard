# Pinned to the major versions verified working on 2026-06-13 (Python 3.12).
# Lower bound = what we tested; upper bound guards against the next major
# (e.g. pandas 4 / streamlit 2) silently breaking the deployed app.
streamlit>=1.58,<2
requests>=2.31,<3
pandas>=3.0,<4
plotly>=6.0,<7
python-dateutil>=2.9,<3
anthropic>=0.60,<1

# Touch to force a full Streamlit Cloud environment rebuild + process restart
# (a plain code push reruns the script but keeps imported modules cached in memory).
# rebuild: 2026-08-06e (decode WPCOM token from OAuth redirect + token fingerprint)
